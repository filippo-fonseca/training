# bgsd — runtime artifacts (local only)
# Ignore all runtime artifacts; commit only the persistent records.
.bgsd/*
!.bgsd/seshs/
!.bgsd/memory/
!.bgsd/ledger.md
!.bgsd/config.json
.bgsd-tmp/

# env files (open-source: never commit)
.env
.env.local
.env.*.local

# deps/build
node_modules/
.next/
