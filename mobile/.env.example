# Public Expo env (safe to ship in the binary; RLS still gates data).
EXPO_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Deployed Next.js origin (no trailing slash). Used for Strava OAuth +
# assembled dashboard/calendar payloads.
EXPO_PUBLIC_API_BASE_URL="https://training-filippo-fonsecas-projects.vercel.app"

# Optional UX gate (RLS remains the real boundary).
EXPO_PUBLIC_OWNER_EMAIL="you@example.com"
